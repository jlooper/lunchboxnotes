
<% @notes.find(1) do |note| %>
    <div class="note" id="note" style="background-image:url('/assets/<%= @random_image%>')">
    	<div class="noteText">
    		<%= note.content %>
    	</div>
    </div>
<% end %>
